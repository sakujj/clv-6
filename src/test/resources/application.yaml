fileName: "application.yaml"

dataSource:
  hikari:
    driverClassName: "org.postgresql.Driver"
    username: "postgres"
    password: "postgres"
    maximumPoolSize: 5
  executeOnStartUp:
    - "database-init/ddl.sql"
dao:
  cache:
    type: "NONE"
    capacity: 0
